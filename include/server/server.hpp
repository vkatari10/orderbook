#pragma once


#include <cstdint>


#include "orderbook/orderbook.hpp"

class Server {
public:

    /** 
     * @brief creates a new server object
     * 
     * @param ticker the ticker this server will serve
     */
    explicit Server(const std::string& ticker);

    





private:
   
};